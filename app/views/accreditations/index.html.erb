<h1>Listing companies</h1>

<table>
  <tr>

  </tr>

<% @accreditations.each do |accreditation| %>
  <tr>
    <td><%= accreditation.niceic_approved_contractor %></td>
    <td><%= accreditation.niceic_domestic_installer %></td>
    <td><%= accreditation.electrical_contractors_association%></td>
    <td><%= accreditation.napit %></td>
    <td><%= accreditation.elecsa %></td>
    <td><%= link_to 'Show', accreditation %></td>
    <td><%= link_to 'Edit', edit_company_path(accreditation) %></td>
    <td><%= link_to 'Destroy', company, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Company', new_accreditation_path %>
