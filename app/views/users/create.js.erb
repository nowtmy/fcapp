<% if @user.errors.any? -%>
  /*Hide the flash notice div*/
  $("#flash_notice").hide(300);

  /*Update the html of the div post_errors with the new one*/
  $("#user_errors").html("<%= escape_javascript(error_messages_for(@contact))%>");

  /*Show the div post_errors*/
  $("#user_errors").show(300);
<% else -%>
  /*Hide the div post_errors*/
  $("#user_errors").hide(300);

  /*Update the html of the div flash_notice with the new one*/
  $("#user_notice").html("<%= escape_javascript(flash[:notice])%>");

  /*Show the flash_notice div*/
  $("#flash_notice").show(300);

  /*Clear the entire form*/
  $(":input:not(input[type=submit])").val("");

  /*Replace the html of the div post_lists with the updated new one*/
  <% unless Rails.env== 'development' %>
  window.top.location.replace("http://fcapp.heroku.com");
  <% end %>
<% end -%>

